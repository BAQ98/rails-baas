<div class="relative">
  <div class="absolute z-10 left-[50%] translate-x-[-50%]">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
        <%= notice %>
      </p>
    <% end %>
  </div>
</div>